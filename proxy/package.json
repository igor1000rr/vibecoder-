{
	"name": "vibecoder-proxy",
	"version": "0.1.0",
	"description": "Cloudflare Worker proxy for Vibecoder IDE - forwards LLM requests to providers (Anthropic, OpenAI, Gemini, OpenRouter)",
	"private": true,
	"main": "src/index.ts",
	"scripts": {
		"dev": "wrangler dev",
		"deploy": "wrangler deploy",
		"tail": "wrangler tail"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20250101.0",
		"typescript": "^5.4.0",
		"wrangler": "^4.0.0"
	},
	"license": "Apache-2.0"
}
